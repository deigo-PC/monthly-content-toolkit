# Coordenadas Content System

A work-in-progress AI-powered system for generating monthly content plans and
producing every deliverable type — carousel prompts, blog posts, infographic
briefs, images, landing pages, and performance reports. Under active
development — expect frequent changes.

Uses the OpenCode skills system. Designed to run unattended on a VPS
via n8n/Hermes, with human approval preserved at the PM review gate.

---

## Current State

**Built and working today:**

| Skill | What it produces | Status |
|---|---|---|
| `content-plan-generator` | Monthly content plan (.md → .html) | ✅ Working |
| `carousel-prompt-generator` | Per-slide AI image prompts (.md → .html) | ✅ Working |
| `blog-post-generator` | Full SEO blog posts (.md → .html) | ✅ Working |
| `infographic-brief-generator` | Design briefs for infographics (.md → .html) | ✅ Working |
| `landing-page-generator` (`monthly-drop-landing-page`) | Deployed Vercel landing page (TanStack) | ✅ Working |

Skills below come from the
[coreyhaines31/marketingskills](https://github.com/coreyhaines31/marketingskills)
catalog — installed into OpenCode by `setup.ps1`.

**Existing externally, not yet ported into this repo:**

| Skill | Status |
|---|---|
| `marketing-brain` | 8-phase pipeline → Marketing Brain HTML + Strategy Doc |
| `hl-subaccount-setup` | GoHighLevel bot/pipeline config |
| `design-sheet-generator` | Per-persona visual reference sheets |
| `ai-video-prompt-writer` | Two-stage AI video prompt pipeline |
| `carousel-slide-processor` | Image post-processing (resize, logo, naming) |

**Planned but not yet built:**

| Skill | Purpose |
|---|---|
| `prompt-improvement-filter` | Rewrites raw prompts against design-sheet tokens + negative exclusions |
| `image-generation` | Executes SOPs via GPT-image-2 API |
| `marketing-hub-generator` | Public hub page per brand |
| `content-scheduler` | API-based scheduling + 2-week buffer check |
| `performance-report-generator` | Monthly performance report from analytics |

---

## Architecture — ICM Layers 0–4

The folder structure follows the **Interpretable Context Methodology (ICM)**:

| Layer | What | Where |
|---|---|---|
| 0 | Brand identity | `Brands/[NAME]/AGENT.md` |
| 1 | Task routing | `Brands/[NAME]/CONTEXT.md` |
| 2 | Reusable skills | `.agents/skills/` (repo root, brand-agnostic) |
| 3 | Stable reference | `_knowledgebase/`, `BrandKit/`, `docs/SOP_reference/` |
| 4 | Working artifacts | `Monthly_Cycles/[NN]-[MMMYY]/` |

Skills live once at repo root — brand-specific behavior goes in `AGENT.md`,
not in a forked skill. SOPs live once at `docs/SOP_reference/`, referenced by
every brand's `image_gen_references/`.

---

## Folder Structure

```
coordenadas-content-system/
│
├── .agents/skills/           # Layer 2 — all skills, brand-agnostic
├── docs/SOP_reference/       # Canonical SOPs for image generation
├── SYNC.md                   # Sync/commit rules
│
└── Brands/
    ├── _template/            # Copy this per new brand
    └── [BRAND_NAME]/
        ├── AGENT.md           # Layer 0 — identity, deviations, credentials
        ├── CONTEXT.md         # Layer 1 — stage routing
        ├── brand_guide.html   # Generated by Claude (before marketing-brain)
        ├── marketing_brain.html / masterStrategyDoc.html
        ├── _knowledgebase/    # Strategy docs, research, sitemap
        ├── BrandKit/          # brand_guide.md, logos, fonts, design_sheets/
        ├── image_gen_references/  # SOP symlinks
        ├── hl_subaccount_config/
        └── Monthly_Cycles/
            ├── [NN]-[MMMYY]/  # e.g. 07-JUL26
            │   ├── CYCLE_CONTEXT.md
            │   ├── 01_content_plan/
            │   ├── 02_prompts_and_copy/
            │   ├── 03_infographic_briefs/
            │   ├── 04_image_generation/
            │   ├── 05_landing_page/
            │   ├── 06_scheduling/
            │   └── 07_report/
            └── AH-[YYYYMMDD]-[slug]/  # Ad-hoc cycles
```

---

## How to Use Today

### Quick Start (one-time bootstrap)

This repo is a bootstrap — you only need it once to install the skills
into OpenCode. After setup, delete the repo — all skills persist in
`~\.config\opencode\skills\`.

```powershell
git clone https://github.com/deigo-PC/monthly-content-toolkit.git
cd monthly-content-toolkit
.\setup.ps1
cd ..
Remove-Item -Recurse -Force monthly-content-toolkit
```

What `setup.ps1` does:

1. Installs 46 marketing skills (`coreyhaines31/marketingskills`) via global npx
2. Moves them from global to OpenCode's directory (`~\.config\opencode\skills\`)
3. Copies the 5 custom skills from the repo into OpenCode
4. Installs the TanStack template dependencies for the landing page skill
5. Deletes the global skills directory — everything now lives in OpenCode

### Verifying the installation

```powershell
Get-ChildItem "$env:USERPROFILE\.config\opencode\skills" | Select-Object Name
```

You should see the 5 custom skills plus 46 marketing skill directories.
OpenCode auto-discovers skills from this directory on launch.

### Prerequisites for the landing page step

| Tool | How |
|---|---|
| GitHub CLI | `winget install GitHub.cli`, then `gh auth login` |
| Vercel | GitHub connected in Vercel (`vercel.com/settings/git` → Add GitHub Integration) |

### Adding a Brand

If you still have the repo cloned:

```powershell
Copy-Item -Path "Brands\_template" -Destination "Brands\NewClient" -Recurse
```

If you deleted the repo, re-clone it temporarily, copy the template,
then delete it again. Or in OpenCode just say:

> "Set up a new brand for [Name]"

OpenCode will prompt for the information it needs (audience, positioning,
brand guide, etc.) and create the brand context.

Fill in the template files:
- `AGENT.md` — brand identity, audience, positioning, credentials
- `BrandKit/brand_guide.md` — visual identity
- `_knowledgebase/` — strategy docs, buyer psychology, FAQs, sitemap

### Running a Monthly Cycle

In OpenCode:

1. **"Generate the content plan for [month] for [ClientName]"**
   → Reads `AGENT.md`, brand guide, knowledgebase
   → Produces content-plan.md → content-plan.html
   → Output: `Monthly_Cycles/[NN]-[MMMYY]/01_content_plan/output/`

2. **"Now generate the carousel image prompts"**
   → Reads content plan + brand guide
   → Produces carousel-prompts.md → carousel-prompts.html
   → Output: `Monthly_Cycles/[NN]-[MMMYY]/02_prompts_and_copy/output/`

3. **"Now write the blog posts"**
   → Reads content plan + knowledgebase + sitemap
   → Produces blog-posts.md → blog-posts.html
   → Output: `Monthly_Cycles/[NN]-[MMMYY]/02_prompts_and_copy/output/`

4. **"Now generate the infographic design briefs"**
   → Reads content plan + brand kit
   → Produces infographic-briefs.md → infographic-briefs.html
   → Output: `Monthly_Cycles/[NN]-[MMMYY]/03_infographic_briefs/output/`

5. **"Build the landing page for [ClientName]"**
   → Requires images generated and named per SOPs
   → Creates TanStack project, deploys to Vercel
   → Output: Public Vercel URL

   **Prerequisites for step 5:** `gh` CLI authenticated to GitHub
   (`gh auth login`) and a Vercel account connected to GitHub
   (vercel.com → Settings → Git → Add GitHub Integration). Run
   `.\setup.ps1` to verify — it checks and guides setup.

---

## Roadmap

See `docs/` for detailed planning documents:

| Document | What it covers |
|---|---|
| `docs/WORKFLOW.md` | Detailed monthly pipeline steps |
| `docs/monthly-loop.md` | Config saving and re-running |
| `BLUEPRINT_Coordenadas_Automation.md` | Full architecture and open questions |
| `EXECUTION_PLAN.md` | Phase-by-phase implementation plan |

The long-term roadmap (from the Execution Plan):

| Phase | What | Timeline (part-time) |
|---|---|---|
| 1 | Team alignment & process walkthrough | ~3–5 days |
| 2 | Infrastructure: VPS, n8n, headless spike | ~1–2 weeks |
| 3 | Content Orchestrator + Prompt Filter | ~2 weeks |
| 4 | Image Generation Automation | ~3–5 weeks |
| 5 | Landing Page + PM Review Gate | ~1–2 weeks |
| 6 | Marketing Hub skill | ~1–2 weeks |
| 7 | Scheduling + Reporting | ~2–4 weeks |
| 8 | Pilot full run + refine | ~2–3 weeks |

---

## Remote

Canonical remote (once org access is ready):
`github.com/coordenadas-co/coordenadas-content-system`

Current remote:
`github.com/deigo-PC/monthly-content-toolkit`
